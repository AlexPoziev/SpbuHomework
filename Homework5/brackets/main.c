#include "stack.h"
#include <stdio.h>

bool bracketsBalance(char *string, int size) {
    Stack *head = NULL;

    for (int i = 0; i < size; ++i) {
        if (string[i] == '{' || string[i] == '(' || string[i] == '[') {
            push(&head, )
        }
    }
}

int main() {

    return 0;
}
